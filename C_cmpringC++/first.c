#include <stdio.h> 

int main(){ 
    char a;
    a='a';
    printf("%d \n",a); //we can use this in C language we can take char and use its ascii as int
    printf("%c \n",a); //this also works in Cpp
    printf("%c \n",a+1);//this add 1 in its ASCII value 97+1=98=b
    

    //here we can use 
    printf("hello world");//only
    return 0;
}