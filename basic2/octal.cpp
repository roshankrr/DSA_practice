#include <iostream>
using namespace std;
int main(){ 
    int num= 010;//this 10 is in now octal state because it accept between
    cout<<num<<endl;//0-7 so it consider the limit0-7
    //10 in octal  is 8 because 0*pow(8,0)+1*pow(8,1)=8
    return 0;
}